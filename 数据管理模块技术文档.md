# 个人学习档案 AI 助理 — 数据管理模块

> 作者：Velix | 2026-07-03

---

## 一、数据库设计

SQLite 关系型数据库，共 **6 张表**，通过外键关联，按用户隔离数据。

```
users (用户)
  │
  ├── courses (课程) ───┬── grades (成绩)
  │                     ├── practice_records (刷题)
  │                     ├── notes (笔记)
  │                     └── mistakes (错题)
```

### 表结构

| 表名 | 核心字段 | 说明 |
|------|---------|------|
| `users` | id, username, password_hash | PBKDF2-SHA256 加盐哈希 |
| `courses` | id, user_id, name | 按用户隔离，用户名+课程名联合唯一 |
| `grades` | id, user_id, course_id, score, total_score, exam_type, exam_date | 支持 6 种考试类型 |
| `practice_records` | id, user_id, course_id, platform, problem_count, correct_count, duration_minutes | 支持多刷题平台 |
| `notes` | id, user_id, course_id, title, content, tags | 支持关键词检索 |
| `mistakes` | id, user_id, course_id, problem_description, correct_answer, mastered, review_count | 追踪掌握状态 |

### 数据隔离

- 所有查询以 `user_id` 为第一条件
- 课程通过 `UNIQUE(user_id, name)` 保证用户间互不干扰
- 成绩/刷题/笔记/错题均通过外键 `course_id` 关联，删除课程不影响历史数据

---

## 二、安全设计

- **密码存储**：PBKDF2-SHA256 加盐哈希，10 万次迭代
- **API 鉴权**：登录后返回 `user_id`，前端 `sessionStorage` 存储，路由守卫拦截未登录请求
- **SQL 注入防护**：全部使用参数化查询（`?` 占位符），无字符串拼接

---

## 三、CRUD 操作

每张表均实现完整增删改查，以成绩表为例：

| 操作 | 函数 | 说明 |
|------|------|------|
| 新增 | `add_grade()` | 自动创建不存在的课程 |
| 查询 | `get_grades()` | 支持按日期范围、课程筛选 |
| 单查 | `get_grade_by_id()` | 联合查询课程名 |
| 修改 | `update_grade()` | 同步更新关联课程 |
| 删除 | `delete_grade()` | 物理删除 |

错题额外支持：

- `mark_mistake_mastered()` — 标记已掌握，自动累加 `review_count`
- 按掌握状态筛选（`mastered=0/1`）

---

## 四、数据统计聚合

`get_summary_stats()` 根据用户选择的考试范围，自动汇总：

```
总课程数 → 总成绩数 → 刷题次数 → 总刷题量 → 正确数 → 笔记数 → 错题数 → 未掌握错题数
```

支持两种模式：

- **全量统计**：不选考试，汇总全部历史数据
- **范围统计**：选择特定考试，自动取该范围的日期区间，汇总区间内所有数据

---

## 五、AI 数据上下文构建

`build_data_context()` 将结构化数据库记录转换为 AI 可读的文本：

```
===== 数据总览 =====
课程数: 5 | 成绩: 12 条 | 刷题: 156 题 | 笔记: 8 篇 | 错题: 23 道（未掌握: 7）

===== 成绩记录 =====
- [2026-06-15] 高等数学 期中: 85/100 (85.0%)
- [2026-06-20] 大学物理 期中: 78/100 (78.0%)

===== 刷题记录 =====
- [2026-06-18] LeetCode | 题量: 20, 正确率: 75%, 耗时: 120min

===== 错题记录 =====
- [2026-06-16] 高等数学 [未掌握] | 多元函数极值判断...
```

此文本注入 DeepSeek API 的 System Prompt 中，确保 AI 基于真实数据作答。

---

## 六、技术要点总结

| 要点 | 实现 |
|------|------|
| 数据库引擎 | SQLite3，零配置，便携部署 |
| 连接管理 | Python contextmanager，自动 commit/rollback |
| 查询安全 | 100% 参数化查询，防 SQL 注入 |
| 数据隔离 | user_id 垂直隔离 + UNIQUE 约束 |
| 密码安全 | PBKDF2-SHA256，10 万迭代，随机盐 |
| AI 数据流 | `DB → 聚合 → 文本上下文 → DeepSeek Prompt → 分析结果` |
